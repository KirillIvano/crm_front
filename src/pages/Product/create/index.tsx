import React from 'react';

import {AdminForm, useFormContext} from '@/admin-lib';

import styles from './styles.module.scss';


const Input = () => {
    const {} = useFormContext();
};

const CreateProduct = () => {
    return (
        <AdminForm

        >

        </AdminForm>
    );
};

export default CreateProduct;
